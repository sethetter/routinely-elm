{
  "name": "routinely",
  "version": "1.0.0",
  "description": "",
  "main": "elm.js",
  "scripts": {
    "start": "npm run build && docker-compose up -d",
    "build": "elm make --warn --yes src/Routinely.elm --output elm.js",
    "clean": "rm -rf elm-stuff elm.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "elm": "^0.18.0",
    "elm-live": "^2.7.5"
  }
}
