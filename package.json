{
  "name": "routinely",
  "version": "1.0.0",
  "description": "",
  "main": "elm.js",
  "scripts": {
    "serve-api": "postgrest api/postgrest.conf",
    "serve-client": "elm live -- --warn --yes src/Routinely.elm --output elm.js",
    "build": "elm make --warn --yes src/Routinely.elm --output elm.js",
    "clean": "rm -rf elm-stuff elm.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "elm": "^0.18.0",
    "elm-live": "^2.7.5"
  }
}
